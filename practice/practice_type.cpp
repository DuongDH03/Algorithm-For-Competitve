#include <bits/stdc++.h>

using namespace std;

int main(){
    ios_base::sync_with_stdio(0);
    cin.tie(NULL);
    cout.tie(NULL);
    
    vector<int> a;
    int val;
    cin >> val;
    a.push_back(val);
    cout << a[0] << "\n";

    return 0;
}