#include <bits/stdc++.h>

using namespace std;

// int t[i]: time the seed i to grow
// greedy: sort: t1 >= t2 >= ... tn
// solution: max{ti + i} + 1, 1 <= i <= n

int main() {
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0); 



    return 0;
}